# Прогнозирование аварийности на дорогах Москвы

Этот проект — пример небольшого веб-приложения, которое по заданным параметрам участка дороги оценивает вероятность ДТП и возможный уровень образующегося затора. Система работает на данных о времени, погоде и плотности трафика. Все расчёты выполняются в модели, обученной на синтетических данных. Проект полностью контейнеризирован — backend, frontend и база данных разворачиваются одной командой.

## 📦 Стек технологий

**Backend**  
Python 3.11  
Django  
scikit-learn  
Gunicorn  

**Frontend**  
Vue 3  
Vite  

**Database**  
PostgreSQL  

**Контейнеризация**  
Docker  
docker-compose  

## 🚀 Запуск проекта

Перед запуском убедитесь, что установлен Docker Desktop (или Docker Engine + docker-compose).

### 1. Клонировать репозиторий
```
git clone https://github.com/<username>/traffic-accident-prediction.git  
cd traffic-accident-prediction
```

### 2. Запустить контейнеры
docker-compose up --build

После сборки появятся три сервиса:

**frontend** — Vue-приложение: http://localhost:5173  
**backend** — Django API: http://localhost:8000  
**db** — PostgreSQL

Backend использует entrypoint-скрипт, который ожидает готовности базы, выполняет миграции и запускает Gunicorn.

## 🧩 Структура проекта
```
traffic-accident-prediction/  
├── backend/              # Django-приложение  
│   ├── predictor/        # Логика API и ML модель  
│   ├── traffic_risk/     # Настройки Django  
│   ├── entrypoint.sh     # Стартовый скрипт  
│   ├── Dockerfile  
│   └── requirements.txt  
├── frontend/             # Vue-приложение  
│   ├── src/  
│   ├── Dockerfile  
│   └── vite.config.js  
├── docker-compose.yml  
└── .gitignore
```

## 🔧 API

Backend предоставляет один основной эндпоинт:

POST /api/predict/

Пример тела запроса:
```
{  
  "road_segment": "Ленинградский проспект",  
  "latitude": 55.8,  
  "longitude": 37.6,  
  "datetime": "2025-12-05T18:30:00+03:00",  
  "weather": "rain",  
  "traffic_density": 0.7  
}
```

Пример ответа:
```
{  
  "probability": 0.42,  
  "probability_percent": 42.0,  
  "cause": "Плохая погода и высокая плотность движения",  
  "jam_level": "существенный затор",  
  "jam_description": "Вероятно замедление движения на нескольких прилегающих участках.",  
  "log_id": 12  
}
```

## 🖥️ Интерфейс

Фронтенд предоставляет форму, где пользователь указывает участок дороги, координаты, дату и время, погодные условия и плотность трафика. После отправки отображается вероятность ДТП, предполагаемая причина и уровень затора.

## 🧠 Модель

Используется LogisticRegression, обученная на синтетических данных. При необходимости модель легко заменить на реальную.

## 🗄️ Данные и сохранение истории

Каждый запрос сохраняется в таблицу PredictionLog в PostgreSQL. Запись включает координаты, время, погоду, плотность трафика и результат прогноза.

## 🔨 Как вносить изменения

Пересобрать backend:
```
docker-compose build backend  
docker-compose up  
```
Пересобрать frontend:  
```
docker-compose build frontend  
docker-compose up  
```
Если изменяются только .vue-файлы, Vite обновляет интерфейс автоматически.

## 📜 Лицензия

Проект создан в учебных целях и может служить примером интеграции Django, ML-модели, Vue и Docker.
